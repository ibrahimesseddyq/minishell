#2
export a
export a+=hamid

-echo $USER$TESTNOTFOUND$HOME$

-prblm in $OLDPWD
cd ..
cd $OLDPWD

-Garbage collector

-Fd leaks // till now no leak but you should test

-Problem in expanding 
#1 export a="      a   b   "
#2 when $ is at the end 

-problem in Pipes // Maybe OK should be tested more
´

<<s andd crtl-D the exit status should be 1 instead of 0

<< dsd
xxxx
// CTRL D should exit b 0 not 258

 
i should handle fd leaks when signaling HEREDOC


// protect NB_DEFAULT from stack_overflow and heap_overflow

// recheck export and unset

change the space by the del in wildcard phase
// error in heredoc printing no such file or dir

// there is a problem also in the functionning of heredoc